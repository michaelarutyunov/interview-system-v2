# Spec 1.1: Project Scaffolding

## Objective
Create the directory structure and project configuration files for interview-system-v2.

## Context
- Reference: ENGINEERING_GUIDE.md Section 3 (Project Structure)
- This is the foundation for all subsequent specs

## Input Files
- `ENGINEERING_GUIDE.md` - Section 3 defines the structure

## Output Files

### Directory Structure
Create all directories:
```
src/
├── __init__.py
├── api/
│   ├── __init__.py
│   └── routes/
│       └── __init__.py
├── core/
│   └── __init__.py
├── domain/
│   ├── __init__.py
│   └── models/
│       └── __init__.py
├── services/
│   └── __init__.py
├── llm/
│   ├── __init__.py
│   └── prompts/
│       └── __init__.py
├── persistence/
│   ├── __init__.py
│   ├── repositories/
│   │   └── __init__.py
│   └── migrations/
└── utils/
    └── __init__.py
config/
├── methodologies/
├── concepts/
└── scoring/
tests/
├── __init__.py
├── unit/
│   └── __init__.py
├── integration/
│   └── __init__.py
└── fixtures/
    ├── concepts/
    └── responses/
scripts/
data/
```

### pyproject.toml
```toml
[project]
name = "interview-system-v2"
version = "0.1.0"
description = "Adaptive interview system for qualitative consumer research"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.109.0",
    "uvicorn[standard]>=0.27.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "aiosqlite>=0.19.0",
    "httpx>=0.26.0",
    "structlog>=24.0.0",
    "pyyaml>=6.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "black>=24.0.0",
    "isort>=5.13.0",
    "mypy>=1.8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.black]
line-length = 100

[tool.isort]
profile = "black"
line_length = 100
```

### .env.example
```
ANTHROPIC_API_KEY=your-api-key-here
LLM_MODEL=claude-sonnet-4-20250514
DATABASE_PATH=data/interview.db
DEBUG=true
```

### .gitignore
```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Project
.env
data/*.db
*.log

# Testing
.pytest_cache/
.coverage
htmlcov/

# Build
dist/
build/
*.egg-info/
```

### README.md
```markdown
# Interview System v2

Adaptive interview system for qualitative consumer research.

## Setup

```bash
# Create virtual environment
python -m venv .venv
source .venv/bin/activate  # Linux/Mac

# Install dependencies
pip install -e ".[dev]"

# Copy environment template
cp .env.example .env
# Edit .env with your API key

# Run tests
pytest

# Start server
uvicorn src.main:app --reload
```

## Documentation

- [PRD](PRD.md) - Product requirements
- [Engineering Guide](ENGINEERING_GUIDE.md) - Technical specifications
```

## Requirements
1. All `__init__.py` files should be empty (just `# noqa`)
2. Directory structure must exactly match ENGINEERING_GUIDE.md Section 3
3. pyproject.toml must include all dependencies from ENGINEERING_GUIDE.md Section 2.1
4. .env.example must include all variables from ENGINEERING_GUIDE.md Appendix

## Verification
```bash
# Check directory structure exists
test -d src/api/routes && echo "✓ API routes dir exists"
test -d src/persistence/migrations && echo "✓ Migrations dir exists"
test -d config/methodologies && echo "✓ Config dirs exist"
test -d tests/unit && echo "✓ Test dirs exist"

# Check key files exist
test -f pyproject.toml && echo "✓ pyproject.toml exists"
test -f .env.example && echo "✓ .env.example exists"
test -f .gitignore && echo "✓ .gitignore exists"
test -f README.md && echo "✓ README.md exists"

# Verify Python package structure
python3 -c "import src" && echo "✓ src is importable"
```

## Success Criteria
- [ ] All directories created
- [ ] All __init__.py files present
- [ ] pyproject.toml valid and complete
- [ ] .env.example contains all required variables
- [ ] `python3 -c "import src"` succeeds
